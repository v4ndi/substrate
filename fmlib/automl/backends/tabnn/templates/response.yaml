# What makes a response task different from the others. Everything else about the
# run is the same and is generated by assembly.py.
score_transform: sigmoid
is_regression: false
model:
  num_classes: 1
  task_type: classification

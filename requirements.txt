# Canonical dependency versions live in pyproject.toml.
# This installs the package in editable mode with every optional backend + dev tools.
#
#   python -m pip install -r requirements.txt
#
# For a leaner install pick extras explicitly, e.g. `pip install -e .[spark]`.
#
# Note: the Spark backend needs a JDK 8/11/17 on PATH (Spark 3.5 breaks on 21+).

-e .[spark,boosting,dev]
